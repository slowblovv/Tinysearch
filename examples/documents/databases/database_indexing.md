# Database Indexing

An index is a data structure that improves the speed of data retrieval in
a database, at the cost of additional storage and slower writes. B-tree
indexes are the most common database index structure. Choosing the right
index is critical for query performance in large databases.
