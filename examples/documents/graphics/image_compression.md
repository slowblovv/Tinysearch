# Image Compression

Image compression reduces the storage size of an image by removing
redundant or perceptually less important data. Lossless compression
preserves all original data, while lossy compression, used by formats
like JPEG, sacrifices some quality for a much smaller file size.
