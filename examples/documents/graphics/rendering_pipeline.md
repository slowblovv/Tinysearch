# The Rendering Pipeline

The graphics rendering pipeline transforms a 3D scene description into a
2D image for display. Stages typically include vertex processing,
rasterization, and pixel shading. Modern GPUs execute this pipeline in
parallel across thousands of processing cores.
