# Reinforcement Learning

Reinforcement learning trains an agent to make decisions by interacting
with an environment and receiving rewards or penalties. Unlike supervised
learning, there are no labeled examples; instead the agent learns a
policy that maximizes cumulative reward over time through trial and error.
