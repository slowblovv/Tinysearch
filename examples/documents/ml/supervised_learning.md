# Supervised Learning Basics

Supervised learning is a machine learning approach where a model learns
from labeled training data. Each training example consists of an input
and a desired output. Common supervised learning algorithms include
linear regression, decision trees, and support vector machines. The goal
is to generalize well to unseen data.
