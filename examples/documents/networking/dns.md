# How DNS Works

The Domain Name System, or DNS, translates human-readable domain names
into IP addresses. DNS is a hierarchical, distributed database of its
own, with root servers, top-level domain servers, and authoritative name
servers cooperating to resolve names across the network.
