# Understanding Network Latency

Network latency is the time it takes for data to travel across a network
from source to destination. Latency is affected by physical distance,
routing, congestion, and the underlying network protocol. Reducing
latency is critical for real-time applications and search engines alike.
